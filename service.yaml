apiVersion: v1
kind: Service
metadata:
  name: http
  labels:
    app: http
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    # the port that this service should serve on
  - port: 80
  selector:
    app: http
    tier: frontend
---
